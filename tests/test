class Example() {
    fun exampleFunction(var x: Int, var y: Int, var b: Boolean): Boolean {
        if (x > y) { 
            return b 
        } else { 
            return false 
        } 
    }
}
