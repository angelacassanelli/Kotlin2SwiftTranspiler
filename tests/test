class Example() {
    fun exampleFunction(): Boolean { 
        var x: Int
        var y: Int
        
        if (x > y) 
            return 10
        else {
            return 10
        }
        
    }
}
