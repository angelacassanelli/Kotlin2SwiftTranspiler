 class Example() {
    var a: Int = 10
    var b: Int = 100
    fun isAGreaterThanB(): Boolean { 
        if (a > b) { 
            return true 
        } else { 
            return false 
        } 
    }
}